# 局域网共享文件池可视化系统 - 产品设计文档

## 1. 产品定位

**一句话定位**：让 NAS 共享文件夹在局域网内「零配置、可视化、可备注」地被所有设备直接访问。

**核心用户**：家庭/小团队 NAS 使用者，厌倦输地址、翻文件夹、记不住文件用途。

**使用场景**：
- 手机拍完照/截图扔进共享池，电脑秒开分类看
- 设计素材/装修图/代码片段混放，按类型标签页切换
- 给文件加备注「给客户用的最终版」「待复盘」，换设备还在

---

## 2. 核心价值主张

| 痛点 | 方案 |
|------|------|
| 每次输 IP + 端口 + 路径 | 单一短链接，全设备直达 |
| 文件平铺靠文件名猜内容 | 按后缀自动分桶 + 缩略图墙 |
| 不知道文件是干啥的 | 点击即写备注，持久化不丢 |
| 手机浏览器操作别扭 | PWA 安装到主屏，离线可看列表 |

---

## 3. 功能范围

### MVP（v0.1 必须交付）
| 模块 | 关键能力 |
|------|----------|
| **单入口直达** | 无登录、无跳转、响应式适配手机/电脑/平板 |
| **分类标签页** | 6 大分类：图片/视频/文档/代码/压缩包/其他，扩展名映射可配置 |
| **网格缩略图** | 图片/视频首帧/PDF 首页/Office 图标/代码高亮缩略图 |
| **文件备注** | 侧边栏编辑，SQLite 持久化，跨设备同步可见 |
| **预览/下载** | 原生预览（图片/视频/PDF/文本），其它强制下载 |
| **PWA 安装** | Manifest + Service Worker，添加到主屏离线可用 |

### 后续迭代（v0.2+ 备选）
- 全文搜索（文件名 + 备注）
- 标签/收藏/最近访问
- 上传/重命名/删除/新建文件夹（仅管理员）
- 多用户只读/读写权限
- WebDAV / SMB 挂载入口
- 服务端缩略图预生成（大文件夹性能优化）

---

## 4. 非功能需求

| 指标 | 目标 |
|------|------|
| **部署复杂度** | 一条 Docker 命令，挂载两个卷（数据目录 + sidecar.db） |
| **首屏加载** | < 2s（千兆局域网，1万文件以内） |
| **并发** | 10 设备同时浏览无感 |
| **存储侵入性** | 零侵入：不改原文件、不建隐藏文件、仅读取 + 写独立 SQLite |
| **跨平台** | Windows/macOS/Linux/NAS (ARM/x86) 单二进制/Docker 均可跑 |
| **维护成本** | 无数据库服务、无外部依赖、二进制升级覆盖即可 |

---

## 5. 技术架构概览

```
┌─────────────────────────────────────┐
│  前端 (Vue 3 + Vite + Tailwind)     │
│  - CategoryView / GridView / List   │
│  - AnnotationPanel (备注侧边栏)      │
│  - PWA (manifest + sw)              │
└──────────────┬──────────────────────┘
               │ HTTP/JSON (REST)
┌──────────────▼──────────────────────┐
│  后端 (Go 单二进制)                  │
│  - chi 路由                          │
│  - fsnotify 增量扫描                 │
│  - better-sqlite3 (sidecar.db)      │
│  - 静态文件直通 (nginx 风格)         │
└──────────────┬──────────────────────┘
               │ 只读挂载
┌──────────────▼──────────────────────┐
│  NAS 共享目录 (/mnt/nas/share)       │
│  - 原始文件不动                      │
│  - sidecar.db 仅存元数据             │
└─────────────────────────────────────┘
```

---

## 6. 数据模型（最小集）

```sql
-- sidecar.db
CREATE TABLE file_meta (
    path        TEXT PRIMARY KEY,      -- 相对共享根目录的路径
    category    TEXT NOT NULL,         -- image/video/document/code/archive/other
    annotation  TEXT DEFAULT '',       -- 用户备注
    updated_at  INTEGER NOT NULL       -- Unix ms，用于增量同步
);

CREATE INDEX idx_category ON file_meta(category);
CREATE INDEX idx_updated ON file_meta(updated_at);
```

扩展名 → 分类映射 `categories.json`（可外置配置热加载）。

---

## 7. 交互流程（核心路径）

1. **首屏**：打开地址 → 后端返回目录树扁平列表 → 前端按 `category` 分桶渲染 6 个标签页
2. **浏览**：点标签页 → 网格懒加载缩略图（IntersectionObserver）
3. **备注**：点卡片 → 右侧/底部滑出面板 → 输入 → `PATCH /api/meta/:path` → 乐观更新 UI
4. **预览**：点缩略图 → 新标签页 `GET /files/:path`（Range 请求支持视频拖拽）
5. **安装**：浏览器菜单「添加到主屏幕」→ Service Worker 缓存 `index.html` + 列表 JSON → 离线可看

---

## 8. 里程碑

| 版本 | 时间箱头 | 交付物 |
|------|----------|--------|
| **v0.1 MVP** | 2-3 天 | 可跑通上述 6 项核心功能，Docker 一键部署 |
| **v0.2 体验** | +1 周 | 搜索、标签、收藏、键盘快捷键、深色模式 |
| **v0.3 协作** | +2 周 | 只读/读写 Token、上传、WebDAV 只读挂载 |
| **v1.0 稳定** | +1 周 | 性能调优、文档、自动更新、多架构镜像 |

---

## 9. 验收清单（Definition of Done）

- [ ] 手机/电脑/平板同一 URL 直达，无登录
- [ ] 6 大分类标签页切换流畅，文件自动归桶正确
- [ ] 缩略图加载无白屏闪烁，视频首帧可见
- [ ] 备注写入/读取/跨设备同步零丢失
- [ ] PWA 安装后离线打开能看到已缓存列表
- [ ] Docker 命令一行跑起，挂载目录即用
- [ ] 单元测试覆盖：扫描分类、备注 CRUD、API 路由
- [ ] 代码通过 `go vet` / `npm run lint` / `npm run typecheck`

---

## 10. 风险与对策

| 风险 | 对策 |
|------|------|
| 文件数 > 5 万首屏慢 | 增量扫描 + 分页懒加载 + 可选 Meilisearch |
| 视频缩略图生成慢 | 前端直接 `<video preload="metadata">` 抽首帧，不做服务端转码 |
| 备注冲突（多端同改） | 乐观锁 `updated_at`，冲突提示合并 |
| NAS 休眠导致挂载断开 | 前端轮询心跳，断线重连 Toast 提示 |